#include <bits/stdc++.h>
using namespace std;

int main()
{
    int arr[5];
    cout << "Enter elements of the array : ";
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];

    cout << endl;

    cout << "The elements are : " << arr[0] << " " << arr[1] << " " << arr[2] << " " << arr[3] << " " << arr[4] << " " << endl;

    return 0;
}